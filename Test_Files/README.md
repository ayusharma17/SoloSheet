# Synthetic upload fixtures

This directory may contain only self-authored or explicitly redistributable test
material. The tracked PDF is generated from repository-owned text by running:

```sh
npm run fixtures:generate
```

Do not add lecture slides, textbooks, assignments, annotations, student work, or
other third-party documents. Use local untracked files when a manual size or
format test cannot be represented by the synthetic fixture.
